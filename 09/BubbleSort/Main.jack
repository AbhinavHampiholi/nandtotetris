class Main{
    function void main(){
        var Array a; 
        var int length;
        var int i;

      let length = Keyboard.readInt("How many numbers? ");
      let a = Array.new(length); // constructs the array
     
      let i = 0;
      while (i < length) {
         let a[i] = Keyboard.readInt("Enter a number: ");
         let i = i + 1;
      }

        do Sort.bubble(a, length);
        return;
    }
}